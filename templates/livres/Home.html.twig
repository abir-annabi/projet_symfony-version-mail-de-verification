{% extends 'base.html.twig' %}

{% block title %}Kteby - Librairie d'exception{% endblock %}

{% block stylesheets %}
<style>
    /* Styles spécifiques à la page d'accueil */
    .hero-section {
        background: linear-gradient(rgba(245, 233, 224, 0.9), rgba(245, 233, 224, 0.7)), 
                    url('https://images.unsplash.com/photo-1495446815901-a7297e633e8d?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
        background-size: cover;
        background-position: center;
        height: 70vh;
        display: flex;
        align-items: center;
        text-align: center;
        color: var(--noir);
    }

    .hero-content {
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        line-height: 1.2;
    }

    .hero-subtitle {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        font-weight: 300;
    }

    .btn-primary-custom {
        background-color: var(--marron);
        color: var(--blanc-creme);
        padding: 0.8rem 2.5rem;
        border-radius: 0;
        font-size: 1.1rem;
        letter-spacing: 1px;
        transition: all 0.3s ease;
        border: 2px solid var(--marron);
    }

    .btn-primary-custom:hover {
        background-color: transparent;
        color: var(--marron);
    }

    .feature-section {
        padding: 5rem 0;
        background-color: var(--blanc-creme);
    }

    .section-title {
        font-size: 2.5rem;
        text-align: center;
        margin-bottom: 3rem;
        position: relative;
    }

    .section-title::after {
        content: '';
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 2px;
        background-color: var(--marron);
    }

    .feature-card {
        background: white;
        padding: 2rem;
        margin: 1rem;
        text-align: center;
        box-shadow: var(--ombre-douce);
        transition: transform 0.3s ease;
        height: 100%;
        border: 1px solid var(--taupe);
    }

    .feature-card:hover {
        transform: translateY(-10px);
    }

    .feature-icon {
        font-size: 2.5rem;
        color: var(--marron);
        margin-bottom: 1.5rem;
    }

    .feature-title {
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    .quote-section {
        background-color: var(--taupe);
        padding: 4rem 0;
        text-align: center;
    }

    .quote-text {
        font-size: 1.8rem;
        font-style: italic;
        max-width: 800px;
        margin: 0 auto 2rem;
        color: var(--noir);
    }

    .quote-author {
        font-size: 1.2rem;
        color: var(--marron);
    }

    .new-arrivals {
        padding: 5rem 0;
        background-color: var(--beige);
    }

    .book-card {
        background: white;
        margin-bottom: 2rem;
        box-shadow: var(--ombre-douce);
        transition: all 0.3s ease;
        height: 100%;
    }

    .book-card:hover {
        box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }

    .book-cover {
        height: 300px;
        object-fit: cover;
        width: 100%;
    }

    .book-info {
        padding: 1.5rem;
    }

    .book-title {
        font-size: 1.3rem;
        margin-bottom: 0.5rem;
    }

    .book-author {
        color: var(--marron);
        font-size: 1rem;
        margin-bottom: 1rem;
    }

    .newsletter-section {
        background-color: var(--noir);
        color: var(--beige);
        padding: 4rem 0;
        text-align: center;
    }

    .newsletter-title {
        font-size: 2rem;
        margin-bottom: 1.5rem;
    }

    .newsletter-form {
        max-width: 500px;
        margin: 0 auto;
    }

    .form-control-custom {
        border-radius: 0;
        border: 1px solid var(--taupe);
        padding: 0.8rem;
    }

    .btn-newsletter {
        background-color: var(--gold);
        color: var(--noir);
        border: none;
        padding: 0.8rem 2rem;
        margin-top: 1rem;
    }
</style>
{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="hero-section">
    <div class="hero-content">
        <h1 class="hero-title">Bienvenue chez Kteby</h1>
        <p class="hero-subtitle">Découvrez notre collection de livres rares et d'éditions exclusives</p>
        <a href="{{ path('app_livre_all1') }}" class="btn btn-primary-custom">Explorer la collection</a>
    </div>
</section>

<!-- Features Section -->
<section class="feature-section">
    <div class="container">
        <h2 class="section-title">Pourquoi choisir Kteby</h2>
        <div class="row">
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="bi bi-book"></i>
                    </div>
                    <h3 class="feature-title">Sélection rigoureuse</h3>
                    <p>Des livres choisis avec soin par notre équipe de bibliophiles passionnés.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="bi bi-gem"></i>
                    </div>
                    <h3 class="feature-title">Éditions exclusives</h3>
                    <p>Accès à des éditions limitées et des ouvrages difficiles à trouver ailleurs.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="bi bi-headset"></i>
                    </div>
                    <h3 class="feature-title">Conseils personnalisés</h3>
                    <p>Nos experts sont à votre disposition pour vous guider dans vos choix.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Quote Section -->
<section class="quote-section">
    <div class="container">
        <p class="quote-text">"Un livre est une fenêtre par laquelle on s'évade, un miroir où l'on se découvre, et une porte que l'on ouvre pour se retrouver."</p>
        <p class="quote-author">- Citation littéraire</p>
    </div>
</section>


{% endblock %}